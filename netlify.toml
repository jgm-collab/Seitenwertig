[build]
  functions = "netlify/functions"
  publish = "."

[functions]
  node_bundler = "esbuild"

# Background Functions: Endung "-background" im Dateinamen aktiviert automatisch
# den Background-Modus (max. 15 min Laufzeit, sofortiges 202).

# Falls die Briefing-Seite auf einem anderen Pfad liegen soll, hier rewrite einrichten:
# [[redirects]]
#   from = "/briefing"
#   to = "/briefing.html"
#   status = 200
